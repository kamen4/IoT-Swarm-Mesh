# config

Configuration metadata and normalization logic.

Files:
- parameter_ranges.py: declared ranges and parameter types
- config_normalizer.py: clamping, precision rounding, cross-parameter constraints
