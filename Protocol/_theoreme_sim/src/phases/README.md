# phases

Per-round protocol phases used by the simulation step pipeline.

## Files

- downRoundPhase.js - Simulate one DOWN packet round with hybrid forwarding and duplicate tracking.
- upRoundPhase.js - Simulate one UP packet sweep from devices toward the gateway.
- decayPhase.js - Apply periodic DECAY epoch according to configured interval and percent.
