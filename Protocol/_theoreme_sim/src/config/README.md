# config

Configuration metadata and random parameter generation utilities.

## Files

- parameterRanges.js - Declarative range and type map for simulation parameters.
- randomConfigGenerator.js - Generates fully random config values using parameter ranges.
- configNormalizer.js - Clamps and normalizes cross-parameter constraints for stable runs.
